<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" android:minSdkVersion="33" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"

        android:maxSdkVersion="32" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Onlykids_mobile_application"
        tools:targetApi="31">
        <activity
            android:name=".PerformanceReportActivity"
            android:exported="false" />
        <activity
            android:name=".Splash_screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".preclass_question"
            android:exported="false">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".Retry_mistake_memo"
            android:exported="false" />
        <activity
            android:name=".Mistake_memo"
            android:exported="false" />
        <activity
            android:name=".quizGame_result"
            android:exported="false" />
        <activity
            android:name=".start_quiz"
            android:exported="false"></activity>
        <activity
            android:name=".quizGame"
            android:exported="false"></activity>
        <activity
            android:name=".chatRoom"
            android:exported="false"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="false">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity> <!-- <activity android:name=".PreClassActivity" /> -->
        <!-- <activity android:name=".NotesActivity" /> -->
        <!-- <activity android:name=".MistakeMemoActivity" /> -->
        <!-- <activity android:name=".PerformanceReportActivity" /> -->
        <activity android:name=".VideoSearchActivity" />
        <activity android:name=".NotesActivity" />
        <activity android:name=".ViewNotesActivity" />
        <activity android:name=".NoteChatBoxActivity" />
    </application>

</manifest>